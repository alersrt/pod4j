apiVersion: v1
kind: Pod
metadata:
  name: nginx
spec:
  hostNetwork: true
  containers:
    - name: nginx
      image: docker.io/nginx:latest
      ports:
        - containerPort: 80
          protocol: TCP
          hostPort: 80